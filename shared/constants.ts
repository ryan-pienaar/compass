/** Port the API listens on during `pnpm dev` (Vite proxies /api to it). */
export const DEV_API_PORT = 4318;
/** Port the production server (UI + API) listens on for `pnpm start`. */
export const PROD_PORT = 4317;
